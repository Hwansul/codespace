% storybook, component

# Storybook initialize. If you need typescript support, run yarn add --dev typescript.
yarn dlx sb init --builder=vite

# Migrate storybook 7.0 automatically.
yarn dlx stroybook automigrate
